
## Package Manager

The GoAI Timeline project exclusively uses `pnpm` as its package manager. Please ensure you use `pnpm` for all dependency management operations (installing, adding, removing packages).
